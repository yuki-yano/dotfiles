interface:
  display_name: "Goal Setter"
  short_description: "Shape rough work into Codex-ready goals"
  default_prompt: "Use $goal-setter to clarify, lightly explore, and draft a compact /goal with evidence, constraints, and stop rules. Make the running Goal spawn subagents for independent exploration or verification when they can change Done; create separate tasks only when I explicitly request them."

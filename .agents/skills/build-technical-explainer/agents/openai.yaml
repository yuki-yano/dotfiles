interface:
  display_name: "Technical Explainer"
  short_description: "調査・監査結果を意味付き図表・画像を含む静的HTML資料へ変換"
  default_prompt: "$build-technical-explainer を使って、この調査結果を出典と判断根拠が追え、必要な関係は図で理解できるコンパクトなHTML説明資料にしてください。"
policy:
  allow_implicit_invocation: true

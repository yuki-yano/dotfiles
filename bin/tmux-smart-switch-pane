#!/bin/bash
#
# Cursor-aware pane selection for tmux (Refactored version)
# This script selects a pane based on the cursor position when moving between panes
#
# Usage: tmux-smart-switch-pane <direction>
# where <direction> is one of: left, right, up, down, L, R, U, D
#
set -euo pipefail

# Constants
readonly SCRIPT_NAME=$(basename "$0")
readonly INFINITE_DISTANCE=999999

# Global variables for current pane state
DIRECTION=""
CURRENT_PANE=""
CURSOR_X=""
CURSOR_Y=""
PANE_TOP=""
PANE_HEIGHT=""
PANE_LEFT=""
PANE_WIDTH=""
ABSOLUTE_CURSOR_X=""
ABSOLUTE_CURSOR_Y=""
PANES_INFO=""

# Display usage information
usage() {
    cat >&2 <<EOF
Usage: $SCRIPT_NAME <direction>
  where <direction> is one of: left, right, up, down, L, R, U, D

This script performs cursor-aware pane selection in tmux.
When moving between panes, it selects the pane that best aligns
with the current cursor position.
EOF
    exit 1
}

# Normalize direction input to single letter
normalize_direction() {
    local input="${1:-}"
    [ -z "$input" ] && usage
    
    case "$input" in
        left|Left|LEFT|l|L)  echo "L" ;;
        right|Right|RIGHT|r|R) echo "R" ;;
        up|Up|UP|u|U)    echo "U" ;;
        down|Down|DOWN|d|D)  echo "D" ;;
        *)
            echo "Error: Invalid direction: $input" >&2
            usage
            ;;
    esac
}

# Initialize pane information
init_pane_info() {
    # Get all panes information including active pane cursor and command in one call
    PANES_INFO=$(tmux list-panes -F "#{pane_id} #{pane_active} #{cursor_x} #{cursor_y} #{pane_left} #{pane_top} #{pane_width} #{pane_height} #{pane_current_command}")

    # Extract active pane state from list output
    while IFS=' ' read -r pane_id pane_active cursor_x cursor_y pane_left pane_top pane_width pane_height pane_cmd; do
        if [ "$pane_active" = "1" ]; then
            CURRENT_PANE=$pane_id
            CURSOR_X=$cursor_x
            CURSOR_Y=$cursor_y
            PANE_TOP=$pane_top
            PANE_HEIGHT=$pane_height
            PANE_LEFT=$pane_left
            PANE_WIDTH=$pane_width
            break
        fi
    done <<< "$PANES_INFO"

    if [ -z "$CURRENT_PANE" ]; then
        echo "Error: Failed to determine active pane" >&2
        exit 1
    fi

    # Calculate absolute cursor positions in window coordinates
    ABSOLUTE_CURSOR_X=$((PANE_LEFT + CURSOR_X))
    ABSOLUTE_CURSOR_Y=$((PANE_TOP + CURSOR_Y))
}

# Check if value is within range [start, start+length)
in_range() {
    local value=$1
    local start=$2
    local length=$3
    local end=$((start + length))
    
    [ "$value" -ge "$start" ] && [ "$value" -lt "$end" ]
}

# Check if pane is in the specified direction from current pane
is_pane_in_direction() {
    local direction=$1
    local pane_left=$2
    local pane_top=$3
    local pane_width=$4
    local pane_height=$5
    
    case "$direction" in
        L) [ "$pane_left" -lt "$PANE_LEFT" ] ;;
        R) [ "$pane_left" -gt "$PANE_LEFT" ] ;;
        U) [ "$pane_top" -lt "$PANE_TOP" ] ;;
        D) [ "$pane_top" -gt "$PANE_TOP" ] ;;
    esac
}

# Check if cursor position aligns with target pane
cursor_aligns_with_pane() {
    local direction=$1
    local pane_left=$2
    local pane_top=$3
    local pane_width=$4
    local pane_height=$5
    
    case "$direction" in
        L|R) # Horizontal movement: check vertical alignment
            in_range "$ABSOLUTE_CURSOR_Y" "$pane_top" "$pane_height"
            ;;
        U|D) # Vertical movement: check horizontal alignment
            in_range "$ABSOLUTE_CURSOR_X" "$pane_left" "$pane_width"
            ;;
    esac
}

# Check if command name corresponds to Neovim/Vim
is_neovim_command() {
    local pane_cmd=$1
    [[ "$pane_cmd" =~ ^n?vim?$ ]]
}

# Get pane geometry and command from cached panes info
get_pane_geometry_and_command() {
    local target_pane=$1

    while IFS=' ' read -r pane_id pane_active pane_cursor_x pane_cursor_y pane_left pane_top pane_width pane_height pane_cmd; do
        if [ "$pane_id" = "$target_pane" ]; then
            echo "$pane_left $pane_top $pane_width $pane_height $pane_cmd"
            return 0
        fi
    done <<< "$PANES_INFO"

    return 1
}

# Find best pane and edge pane in specified direction (single pass)
find_best_and_edge_pane() {
    local direction=$1
    local best_pane=""
    local best_distance=$INFINITE_DISTANCE
    local edge_pane=""
    local best_position

    # Initialize based on direction (opposite edge)
    case "$direction" in
        L) best_position=-1 ;;           # Find rightmost
        R) best_position=$INFINITE_DISTANCE ;;  # Find leftmost
        U) best_position=-1 ;;           # Find bottommost
        D) best_position=$INFINITE_DISTANCE ;;  # Find topmost
    esac

    while IFS=' ' read -r pane_id pane_active pane_cursor_x pane_cursor_y pane_left pane_top pane_width pane_height pane_cmd; do
        # Skip current pane
        [ "$pane_id" = "$CURRENT_PANE" ] && continue

        # Check if cursor aligns with this pane
        if cursor_aligns_with_pane "$direction" "$pane_left" "$pane_top" "$pane_width" "$pane_height"; then
            local position

            # Get position based on direction
            case "$direction" in
                L) position=$((pane_left + pane_width)) ;;  # Right edge
                R) position=$pane_left ;;                     # Left edge
                U) position=$((pane_top + pane_height)) ;;   # Bottom edge
                D) position=$pane_top ;;                      # Top edge
            esac

            # Update edge pane based on direction
            case "$direction" in
                L|U) # Looking for maximum position
                    if [ "$position" -gt "$best_position" ]; then
                        best_position=$position
                        edge_pane=$pane_id
                    fi
                    ;;
                R|D) # Looking for minimum position
                    if [ "$position" -lt "$best_position" ]; then
                        best_position=$position
                        edge_pane=$pane_id
                    fi
                    ;;
            esac

            # Check if pane is in the correct direction
            if is_pane_in_direction "$direction" "$pane_left" "$pane_top" "$pane_width" "$pane_height"; then
                local distance
                case "$direction" in
                    L) distance=$((PANE_LEFT - (pane_left + pane_width))) ;;
                    R) distance=$((pane_left - (PANE_LEFT + PANE_WIDTH))) ;;
                    U) distance=$((PANE_TOP - (pane_top + pane_height))) ;;
                    D) distance=$((pane_top - (PANE_TOP + PANE_HEIGHT))) ;;
                esac

                # Only consider non-negative distances closer than current best
                if [ "$distance" -ge 0 ] && [ "$distance" -lt "$best_distance" ]; then
                    best_distance=$distance
                    best_pane=$pane_id
                fi
            fi
        fi
    done <<< "$PANES_INFO"

    printf '%s\t%s\n' "$best_pane" "$edge_pane"
}

# Setup Neovim integration and switch pane in a single tmux command
# Returns:
#   0 if target pane is Neovim and switch is performed here
#   1 otherwise
setup_neovim_integration_and_switch() {
    local target_pane=$1
    local is_cycle=$2

    local target_pane_info
    target_pane_info=$(get_pane_geometry_and_command "$target_pane") || return 1

    local dest_pane_left
    local dest_pane_top
    local dest_pane_width
    local dest_pane_height
    local pane_cmd
    read -r dest_pane_left dest_pane_top dest_pane_width dest_pane_height pane_cmd <<< "$target_pane_info"

    if ! is_neovim_command "$pane_cmd"; then
        return 1
    fi

    # Calculate cursor position as percentage within target pane
    local relative_cursor_y=$((ABSOLUTE_CURSOR_Y - dest_pane_top))
    local relative_cursor_x=$((ABSOLUTE_CURSOR_X - dest_pane_left))

    # Ensure cursor Y is within bounds
    if [ "$relative_cursor_y" -lt 0 ]; then
        relative_cursor_y=0
    elif [ "$relative_cursor_y" -ge "$dest_pane_height" ]; then
        relative_cursor_y=$((dest_pane_height - 1))
    fi

    # Ensure cursor X is within bounds
    if [ "$relative_cursor_x" -lt 0 ]; then
        relative_cursor_x=0
    elif [ "$relative_cursor_x" -ge "$dest_pane_width" ]; then
        relative_cursor_x=$((dest_pane_width - 1))
    fi

    # Calculate percentages
    local cursor_percent_y=$((relative_cursor_y * 100 / dest_pane_height))
    local cursor_percent_x=$((relative_cursor_x * 100 / dest_pane_width))

    # Set environment variables first, then switch pane.
    # This avoids a race where Neovim reads env values immediately on focus.
    tmux set-environment NVIM_CURSOR_Y "$cursor_percent_y" \; \
        set-environment NVIM_CURSOR_X "$cursor_percent_x" \; \
        set-environment NVIM_SELECT_DIRECTION "$DIRECTION" \; \
        set-environment NVIM_IS_CYCLE "$is_cycle" \; \
        select-pane -t "$target_pane"

    return 0
}

# Switch to specified pane with optional Neovim integration
switch_to_pane() {
    local target_pane=$1
    local is_cycle=$2
    
    if [ -n "$target_pane" ]; then
        if ! setup_neovim_integration_and_switch "$target_pane" "$is_cycle"; then
            tmux select-pane -t "$target_pane"
        fi
    else
        # Fallback to standard tmux navigation
        tmux select-pane -"$DIRECTION"
    fi
}

# Main execution
main() {
    # Validate and normalize direction
    DIRECTION=$(normalize_direction "${1:-}")
    
    # Initialize pane information
    init_pane_info
    
    # Find best pane and edge pane in a single pass
    local pane_result
    pane_result=$(find_best_and_edge_pane "$DIRECTION")

    local target_pane
    local edge_pane
    IFS=$'\t' read -r target_pane edge_pane <<< "$pane_result"

    if [ -n "$target_pane" ]; then
        # Found a suitable pane in the direction
        switch_to_pane "$target_pane" "false"
    else
        # No pane found in direction, try cycling to opposite edge
        switch_to_pane "$edge_pane" "true"
    fi
}

# Execute main function with all arguments
main "$@"

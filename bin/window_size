#!/bin/bash

echo "
tell application \"System Events\"
  tell process \"$1\"
    set topWindow to item 1 of (every window whose focused is true)
    set size of topWindow to {$2, $3}
  end tell
end tell
" | osascript -
